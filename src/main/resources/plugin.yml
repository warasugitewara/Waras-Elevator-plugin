name: WarasElevators
version: '${version}'
main: com.warasugitewara.elevator.ElevatorPlugin
api-version: '1.21'
author: warasugitewara
description: "Wara's Elevators: Walk onto stacked blocks and jump/sneak to ride elevators."
commands:
  elevator:
    description: Manage Wara's Elevators settings.
    usage: /elevator <block|sound|info|reload>
    aliases: [el]
permissions:
  elevator.use:
    description: Allows using elevators.
    default: true
  elevator.admin:
    description: Allows managing elevator configuration.
    default: op
